specVersion: "v1alpha"
serviceId: "local"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "dataconnect"
      cloudSql:
        instanceId: "lon-next"
connectorDirs: ["./connector"]
