{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "server": "localhost",
            "driver": "PostgreSQL",
            "name": "postgresql://user:mypassword@localhost:5432/dataconnect?sslmode=disable",
            "connectString": "postgresql://user:mypassword@localhost:5432/dataconnect?sslmode=disable"
        }
    ]
}